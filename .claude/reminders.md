When writing code remember:
- Maximize code modularity. Prefer small discrete functions with clear intended purpose. Code == building blocks.
- Reuse code from library/ for stack-agnostic language code. Extend library/ when it makes sense.
- Reuse code from core/ for app agnostic common functions. Extend core/ when it makes sense.